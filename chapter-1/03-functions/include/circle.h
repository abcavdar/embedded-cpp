#include <constants.h>

// Calculates the area of circle whose radius is given as first parameter.
double AreaOfCircle(const double &radius);
// Calculates the perimeter of circle whose radius is given as first parameter.
double PerimeterOfCircle(const double &radius);
