#include <iostream>

void HelloWorld() { 
    // Function body
    std::cout << "Hello World!" << std::endl; 
}

int main() {
  HelloWorld();  // Function call

//   int x{}, y{};

//   std::cout << "x and y before cin: " << x << " " << y << std::endl;
//   std::cout << "Enter two integers that will be added to each other with "
//                "spaces between them: ";
//   std::cin >> x >> y;

//   std::cout << "x and y before Add: " << x << " " << y << std::endl;
//   int sum = Add(x, y);  // Function call
//   std::cout << "Sum: " << sum << std::endl;
//   std::cout << "x and y after Add: " << x << " " << y << std::endl;

  return 0;
}

// int Add(int a, int b) {
//     int delta = 5;
//     a += delta;  // a = a + delta;
//     b += delta;
  
//     int result = a + b;
  
//     return result;
//   }
  